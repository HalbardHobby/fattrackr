<nav class="navbar is-dark" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <%= link_to "FatTrackr", root_path, method: :get, class: "navbar-item" %>

    <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarMain" class="navbar-menu">
  
    <div class="navbar-start">
      <% if user_signed_in? %>
        <%= link_to "Expenses", expenses_path, class: "navbar-item" %>
        <%= link_to "Intakes", intakes_path, class: "navbar-item" %>
      <% end %>
    </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <% if user_signed_in? %>
          <div class="buttons">
            <%= link_to "Log out", destroy_user_session_path,
                method: :delete, class: "button is-light" %>
          </div>
        <% else %>
          <div class="buttons">
            <%= link_to "Sign Up", new_user_registration_path,
                method: :get, class: "button is-primary" %>
            <%= link_to "Log In", new_user_session_path,
              method: :get, class: "button is-light" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>